# Changes to be made to the prototype

- Add a file system database (sqlite). The database stores the csv data som that the manager doesn't have to keep uploading unless uploading a new csv.
- Add a Login System for one fund manager. Lets assume the application is only used by one. Use the email h220021hit.ac.zw The name is "Lorraine Mujuru" for this Manager. I want this user to be in environment variables not the database. The defualt password is "lorraine" Dont use bcrypt
- Fund IDs are too Vag. Lets give automatic names to these funds. Keep the IDs but please add a name. You could modify the dataset or do it in the app.
- Remove everything to do with inflation in the application
- Simplify the System so that normal users can use it. It is too academic and research based.
- Functionality to help pension fund managers
- Most importantly Make sure the User Interface is very nice.